[package]
name = "fns-client-headless"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { workspace = true }
fns-config = { workspace = true }
fns-sync-engine = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
